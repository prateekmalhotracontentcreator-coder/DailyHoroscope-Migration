# Handover Brief -- 2026-05-14
> Supersedes: HANDOVER_2026-05-13.md
> Session focus: 18-Module reconciliation decisions, Watch-Outs Sprint R1 (6 tasks), Repo Cleaning Sprint (5 phases), .claude/ restructure, Audit findings clearance

---

## What Is Live (Updated 2026-05-14)

| Module | Status | Notes |
|---|---|---|
| LK Standalone (Onboard / Report / Tracker / Browse) | ✅ LIVE | On-page SEO added |
| All 5 Remedy Modules (Dana / Gemstones / Crystal / Chakra / Mantra) | ✅ LIVE | On-page SEO added |
| The Strategist (War Room / Missions / Report / Surrogate) | ✅ LIVE | Landing at /the-strategist |
| KP Oracle (`/krishna-prashnavali`) | ✅ LIVE | Bundle-native remedies |
| Premium / Free tier system | ✅ LIVE | 16+ routes behind PremiumRoute |
| Lagna Kundali | ✅ LIVE | On-page SEO added |
| On-page SEO -- all 13 modules | ✅ LIVE | |
| Smart-quote pre-commit hook | ✅ LIVE | `scripts/sanitise-smart-quotes.sh` |
| Legal pages (code) | ✅ READY | `/terms` `/privacy` `/subscription-terms` `/refund-policy` `/cookie-policy` -- MongoDB empty |
| Punya Rewards | ✅ BUILT | Route pending user confirmation before App.js wire |

---

## This Session -- Completed Work

### Watch-Outs Sprint R1 (6 tasks, 6 commits)

| Task | Commit | What |
|---|---|---|
| R3 Smart-quote hook | `bcf0a7a` | `scripts/sanitise-smart-quotes.sh` + `.git/hooks/pre-commit` |
| R5 KE ingest freeze | `d746d8f` | Freeze banner on KNOWLEDGE_ENGINE_ROADMAP.md, CODEX_MASTER_ROADMAP.md, INGEST_PROCESS_BRIEF.md, PROJECT_STATUS.md |
| R6 Legal seed script | `f6be8d9` | `backend/scripts/seed_policies_v1.py` -- reads 5 .docx from ~/Documents/Everyday Horoscope/, upserts to horoscope_db.policies |
| R4 Premium copy fix | `d6b0e7a` + `1f70926` | PalmistryPage (prior) + TarotPage line 717 + LuminaPage lines 1258/1454 -- removed unsupported live-planet claims |
| R1 Doc consolidation | `94db919` | Archived 12 handovers, 12 commission briefs, 5 root junk files; .claude/_archive/ created |
| R2 Pages restructure | `ce0944f` | 62 flat JSX → 18 named subdirs; all import paths corrected; build verified |

### Repo Cleaning Sprint (5 phases, 5 commits)

| Phase | Commit | What |
|---|---|---|
| A scripts/ archive | `72878e4` | backend/scripts/: 179 → 57 active; 122 one-time scripts archived |
| B codex-deliveries/ | `3027427` | Entire folder (39 files) moved to .claude/_archive/codex-deliveries/ |
| C .claude/ briefs | `7dd9a14` | Strategist + cleanup plan briefs archived; KE Handover removed from root |
| D Root tidy | `bb4670e` | backend_test.py → tests/; design_guidelines.json + Logo.png → docs/; public/ removed |
| E test_reports/ | `35f2950` | 5 iteration JSONs archived |

### .claude/ Restructure by Module (`1bd1b77`)

```
.claude/briefs/
  knowledge-engine/   -- KE master contract + library amendment template
  remedies/           -- 3 remedies files (brief, Phase1 commission, spec)
  longevity/          -- Longevity contract
  kundali/            -- Lagna Kundali contract
  tarot/              -- Tarot UI v2 prompt pack
  panchang/           -- Language pages plan (deferred)
  seo/                -- SEO & web perf brief
  lk/                 -- LK standalone spec
.claude/ke/
  BOOK_STATUS.md, INGEST_PROCESS_BRIEF.md, KNOWLEDGE_VALIDATION_WORKFLOW.md
  ke/ingest/ (9 book ingest guides)
  ke/nlm/   (1 NLM doc)
```

### Audit Findings Clearance (this commit)

- CLAUDE.md §3: Added `auto_approved` vs `approved` distinction (two separate levels)
- CLAUDE.md §3: Added `compute_dasha_timeline` architecture note (reformatter, not calculator)
- CLAUDE.md §7: Updated Current Build Focus (LK + Strategist both live, next = Remedies + Legal)
- CLAUDE.md §9: Updated Platforms Live (all 15 modules listed)
- CLAUDE.md §10: New section -- Premium Gating (pattern, all gated routes, free/logged-out behaviour)
- HANDOVER_2026-05-14.md: This file
- ACCOUNT2_SESSION_START.md: Updated date, live table, pending items, spec file paths

---

## Premium Gating -- Full Route Map

| Route | Gate | Access |
|---|---|---|
| `/horoscope/daily` `/panchang` | None | Public |
| `/gemstone-remedies` `/crystal-therapy` `/blog` | None | Public |
| `/horoscope/weekly` `/horoscope/monthly` | PremiumRoute | Premium only |
| `/birth-chart` `/kundali-milan` `/brihat-kundli` | PremiumRoute | Premium only |
| `/my-reports` `/individual-reports` `/reports` `/love-reports` | PremiumRoute | Premium only |
| `/ritual-engine` `/arc-angel` `/questionnaire` | PremiumRoute | Premium only |
| `/numerology/report/:id` `/tarot/history` | PremiumRoute | Premium only |
| `/lagna-kundali` (x2) | PremiumRoute | Premium only |
| `/tarot` `/numerology` `/palmistry` `/lumina` `/strategist` `/krishna-prashnavali` | Inline PremiumGateCard | Logged-out: SEO landing; Logged-in free: upgrade gate |

`user.is_premium` from `/api/auth/me` → `auth_utils.py` → queries `db.subscriptions`.

---

---

## Session Update -- 2026-05-14 (Evening)

### Completed This Session

| Task | Commit | What |
|---|---|---|
| fix(kp): crash fix -- None in summary_report | `8606c94` | Filter None values from _summary_report dict before Pydantic validation |
| fix(kp): missing useAuth in KrishnaOracleApp | `8606c94` | Added const { user } = useAuth() -- ReferenceError caused whole page to crash |
| fix(kp): question disclaimer | `8606c94` | Amber disclaimer box on question input: Yes/No questions only, with examples |
| fix(home): zodiac DOB card missing on fresh login | `46661d3` | Changed condition from !dobDone to !primarySign -- modal reappears until zodiac is registered |
| fix(longevity): 422 crash on buildPayload | prior session | Destructure city_slug out before spreading form into API payload |
| feat(remedies): GET /api/remedies/ref/{remedy_ref_id} | `898fe8e` | Task #3 -- clean lookup endpoint for KP remedies, approval_status gated |
| feat(arc-angel): Phase 2a fixes | `898fe8e` | Task #11 -- isPremium wired from useAuth(), locked logic fixed (was inverted), QuestionnaireWidget compact embed added |
| feat(reports): /premium-reports public landing | `d893573` | Task #9 -- 5-report tiles, hero, how-it-works, blurred teaser, FAQ JSON-LD schema |
| feat(tarot): v4 Journal + gamification | `d893573` | Task #10 -- Manifestation Journal tab, XP/streak gamification bar, saveIntention(), stats |
| feat(tarot,reports): /the-tarot + /the-longevity-report landings | `8ce0a4f` | TarotLanding + LongevityLanding public SEO pages wired; TarotPage moon phase badge + ritual notes added |
| legal: seed horoscope_db.policies | DB only | 5 policy docs seeded -- terms/privacy/subscription-terms/refund-policy/cookie-policy (113 sections total) |

---

## Session Update -- 2026-05-14 (Late Evening)

### KP Remedies Ingest -- DONE

- Upload: `upserted=0, modified=36` (records existed; re-ran and confirmed)
- Approval status fix: `update_many` promoted all 36 from `temple_reviewed_pending_engine_ingest` → `approved`
- Smoke test: `GET /api/remedies/ref/kp-001` returns full remedy record ✅
- Indexes: pending creation (see below)

Indexes to create (run once against horoscope_db):
```python
await db.krishna_prashnavali_remedies.create_index("remedy_id", unique=True)
await db.krishna_prashnavali_remedies.create_index("answer_id")
await db.krishna_prashnavali_remedies.create_index("approval_status")
```

---

## Pending (Next Session Start)

### KP Remedy -- Engine Fallback (pending from 2026-05-13)
User: "Keep KP specific Remedy as Default; Remedies Engine as Fall Back -- don't keep any field empty."
- Backend (`scriptural_oracle_router.py`): Reinstate conditional `_resolve_kp_remedy_doc` -- call ONLY when `answer.behavioral_remedy` is None/empty AND `answer.remedy_ref` exists
- Frontend (`KrishnaOraclePage.jsx`): No change expected

### Open Commissions (priority order)
1. **Individual Reports** -- ✅ /premium-reports landing done; remaining: Web App tool page immersive redesign (animated section reveals, GlassCard data presentation for `/individual-reports`)
2. **SEO / Technical SEO** -- user to share thread findings
3. **KP Remedy Engine Fallback** -- parked by user; reinstate when ready
4. **Remedies Engine Phase 1** -- ✅ endpoint done, ✅ ingest done; brief at `.claude/briefs/remedies/CODEX_COMMISSION_REMEDIES_ENGINE_PHASE1.md`

### Pending User Decisions
- Strategist 22 records approval -- `approval_status: pending_human_review`; needs user confirmation
- Lagna Kundali tier -- currently Premium; decision pending
- Punya Rewards route -- built (`/punya-rewards`), no App.js wire yet; user reviewing before Temple Team migration
- 5 split-required LK rules -- `lalkitab-ch21-fam-04` + 4 others tagged `split_required=True`

### External Blockers
- WhatsApp: Complete OTP for `+91 96431 10001` in WhatsApp Manager + add payment method to WABA
- Instagram: Resolve Business Account ID loading in Meta Business Manager

---

## KE Ingest Freeze -- Active

No new chapters until KE Phase 1.2 Sprint 2 (arbitration runtime) is delivered.
Validation re-runs on existing batches still allowed.
Current DB: ~4,117+ rules in `horoscope_db`. All at `auto_approved` or `pending_human_review` -- zero `approved`.

---

## Repo State After Today

```
Root: 7 canonical .md files + render.yaml
frontend/src/pages/: 18 named subdirectories, 68 .jsx files
backend/: 71 flat .py files (all routers + utils + engines)
backend/scripts/: 57 active scripts (122 archived in _archive/)
.claude/: 9 flat operational files + briefs/ + ke/ + _archive/
```

---

## Key Architecture Notes (carry forward)

1. ALL live astronomical/dasha data: `vedic_calculator.py` + `pyswisseph` -- never replicate
2. `knowledge_rules` always filtered by `science_id`
3. All notifications via `/api/notifications/trigger/{type}` -- never call push/WA directly
4. Remedies Engine downstream-only for KP -- never overrides KP verdict
5. Commit format: `feat(scope):` / `fix(scope):` / `chore(scope):`
6. Bump `ENGINE_VERSION` in `panchang_router.py` before any backend change
7. All fetch calls: `withCredentials: true` / `credentials: 'include'`
8. Never spread `...form` into API payloads when backend model uses `extra="forbid"`
9. FastAPI 422 `detail` is an array -- always guard with `typeof detail === "string"`
10. COPY, never MOVE when integrating Codex deliveries -- always read Temple file first, paste delta only

---

## Mandatory DB Warning

Two MongoDB databases exist on Render cluster:
- `horoscope_db` -- PRODUCTION (4,117+ rules, current). All ingest targets this.
- `EverydayHoroscope` -- STALE snapshot (~3,200 rules, pre-split-upgrade, 24 Apr 2026). Do NOT use for any operation.

---

## Key Spec Files

```
.claude/HANDOVER_2026-05-14.md              ← THIS FILE (latest)
.claude/ACCOUNT2_SESSION_START.md           ← Quick-start for next session
.claude/MASTER_DECISIONS_18_MODULE_RECONCILIATION_2026-05-14.md
.claude/PREMIUM_COPY_AUDIT_2026-05-14.md
.claude/briefs/remedies/CODEX_COMMISSION_REMEDIES_ENGINE_PHASE1.md
.claude/briefs/knowledge-engine/CODEX_KNOWLEDGE_ENGINE_CONTRACT.md
.claude/ke/BOOK_STATUS.md
```
